# DOC-extractor
AI-powered invoice document extractor that uses multimodal LLMs to automatically extract key fields such as recipient, sender, totals, tax, and account details from PDFs. The system validates outputs via schema and generates visually annotated invoices with bounding boxes for transparent, explainable document intelligence.
